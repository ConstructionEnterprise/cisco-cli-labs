# Lab 2 Live Verification Notes

The live app opened at Lab 2 with SW1 selected, 0/5 checks, and the prompt `SW1>`. Entering the full command `enable` produced `SW1#` and the expected privileged EXEC output. The curriculum selector, named Construction Enterprises / Factory to Foundation topology, device rack, and manual command input were visible and functional.
The live test then accepted `configure terminal` and displayed `SW1(config)#`, followed by `vlan 10` and displayed `SW1(config-vlan)#`. This confirms the refactored app now exposes the correct IOS VLAN configuration submode during manual entry.
